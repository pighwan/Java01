** 실습 
=> 완성 후 카톡으로 실행된 결과 화면 올려주세요  

** Student 클래스 만들기 **
1) private 필드 정의
=> String 번호 sno, 이름 name
     int  java, html, sum

2) 생성자 1개
=> 생성자로 번호, 이름, java, html 을 초기화하고,
    sum 에는 java+html 로 초기화

3) setter/getter
=> 클래스 외부 에서 모든 필드는 볼수 있지만,
     수정은 할 수 없다.

4) toString  

** StudentTest 클래스 만들기

1) Student 인스턴스 ( s1~s5 ) 를 5개 만들고 출력한다.
=> 데이터는 임의로 하면 됩니다 (Scanner 로 입력받지 않아도 됩니다)

2) 위 5개의 Student 인스턴스를 배열에 넣고 성적순으로 출력한다. 

